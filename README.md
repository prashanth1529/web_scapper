# Data Acquisition Scraper

A simple Python project to demonstrate how to acquire data from the web and save it to a structured format (CSV).
1. **Clone the repository:**

   git clone <your-ssh-url-here>
   cd <your-folder-name>
  2.pip install -r requirements.txt
  3.python main.py
